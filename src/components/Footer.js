import React from "react";
import Header from "@/components/Header";
import Heading from "@/components/Headind";

const Footer = () => {
    return (
        <footer>
            <Heading tag={'h2'} text={'Footer is here!!!'}/>
        </footer>
    );
};

export default Footer;